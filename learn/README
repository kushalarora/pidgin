This directory contains the code to do alignment over the generated graph.

The files in this directory are:
    1. freebase_simple_config
        junto config file for freebase and nell relation alignment. The values needs to be changed as per need basis.

    2. nell_simple_config
        junto config file for freebase and NELL relation alignment

    3. yago_simple_config
        junto config for yago and NELL relation alignment

    4. nell_yago_simple_config
        junto config for yago and nell relation alignment.

    5. generate_score.py
        Takes output generated by junto and calculate scores



